import React from 'react'

const Presets = ({color}) => {
    return (
        <div className={`rounded-full border-0 flex p-2 bg-[${color}] hover:cursor-pointer`}>
            <h1>hell</h1>
        </div>
    )
}

export default Presets